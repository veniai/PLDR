# PLDR 当前状态

更新时间：2026-08-31

阶段：**P1 外部关键词发现已扩展为专题化发现/审核工作区；固定网页与 RSS 可靠采集切片已实现，完整 P1 继续推进**。

当前功能、性能和验收的统一基线见 `docs/product/FUNCTIONAL-PERFORMANCE-REQUIREMENTS.md`。P1 不再只以采集器数量判断进度，还必须形成至少一个冻结范围和来源的真实专题包，让分析员能够回答“今天出现了什么、依据是什么、还缺什么”。

## 已完成

1. 总体方案、产品愿景、参考项目调研和 P0 至 P4 路线图。
2. World Monitor 展示方向与退出条件 ADR。
3. Source、Document、Snapshot、Event、Entity、Claim、Evidence、Assessment 数据模型。
4. URL 规范化、正文抽取、SHA-256、来源独立组和证据精确子串校验。
5. 网页与 RSS 导入接口，公共 URL SSRF 防护，并校验每个 HTTP 重定向跳转。
6. 不同来源的相同正文分别保留来源记录，同时使用 `duplicate_of_document_id` 标记内容重复族。
7. HTML 证据报告、证据快照高亮和模型无密钥降级。
8. P0 固定演示规模：16 来源、48 文档、8 事件、24 主张、56 证据、13 独立来源组。
9. Docker 默认仅绑定 127.0.0.1，CORS 默认限制本地界面，管理 reseed 接口默认关闭并支持令牌保护。
10. P0.2 专题指挥栏、事件档案抽屉、筛选联动、主张证据视图、来源资料视图、报告生成和网页/RSS 导入入口。
11. `./scripts/test-p0.sh` 覆盖单元测试、Python 编译检查和前端 JavaScript 语法检查。
12. GitHub Actions 已配置为对 `master` 的 push 和 pull request 运行同一验收；2026-08-27 已核实提交 `7d3f71a`（P0.2）和 `a8a2062`（更新 Actions 运行时）的对应运行均成功。该结论只描述这两次已完成运行，不代表未来提交必然通过。
13. P0.3 持久化待处理采集箱：公共网页、粘贴文本、TXT/Markdown/HTML/PDF 文件和手动 RSS 条目统一入箱，保留原始/提取快照、指纹、状态和错误。
14. 候选 Event、Entity、Claim、Evidence 及关系独立存储；模型与无模型降级均不修改正式对象，证据定位失败不可确认。
15. 人工新建、合并、修改、驳回和撤销流程，入档预览、原子确认、失败回滚、重复确认幂等、机器候选/人工差异/最终对象追踪。
16. 确认后的正式事件档案、快照高亮和 HTML 报告只读取已确认对象，并支持证据回链、未知字段显式展示和桌面/窄屏操作。
17. 专题内的外部关键词发现入口：新闻/一般网页范围、真实检索后端、原始 URL、站点、渠道、标题、摘要/时间和真实空状态；它与确定网页、粘贴文本、本地文件等输入并列，不是筛选已入档事件。
18. Brave Search API REST v1 与 SearXNG 2026.8.22 薄适配层；组件版本、条款和部署边界持久记录，密钥只从环境读取。
19. 外部查询运行、规范化结果、每次选择事件和选择性入箱追踪独立存储；查询历史按专题隔离并可重开，同一 URL 跨查询复用时保留完整 history；检索渠道不是 Source，搜索标题/摘要/排名不是 Evidence。
20. 查询工作区每次请求 10/20 条；Brave 每页最多返回所选数量，SearXNG 的实际页大小由运营方实例决定，PLDR 会在单查询 100 条上限内保留其完整返回页。跨页按规范化 URL 去重，可全选本页或已加载结果，单批最多选择 100 条。上游总数不可信时明确返回未知，不用已加载量冒充总数。
21. 只有选中结果才异步逐条进入抓取、候选生成和 P0.3 审核队列；每项分别显示排队、抓取、生成、待审核或失败，一项失败不吞掉整批进度。重复选择/确认幂等，正式区在确认前不变。
22. 搜索、抓取与候选失败采用结构化错误，显示阶段、影响、是否可重试、建议动作和追踪 ID；超时/限流可恢复，DNS/SSRF 策略拒绝默认 fail-closed 且不提供无意义的原样重试。
23. 固定公共网页 `CollectionTarget` 与持久化 `CollectionRun`：手动、到期和重试先入队，单 worker 领取，记录 queued/running/succeeded/failed、触发方式、耗时、错误、响应元数据、哈希和租约恢复。
24. 有界流式抓取、文本媒体类型限制、逐跳公共 URL 校验；首次正文形成 V1，提取正文未变不重复建待审项，变化形成后续不可变版本和确定性 Diff。
25. 采集成功与候选生成状态分离；版本仍复用 P0.3 的候选隔离、重新生成、预览、人工确认和负向不污染边界。
26. 同一 canonical URL 的新确认正文复用正式 Document、追加完整 Snapshot；Document 表示最新确认头，新旧 Evidence 和报告链接分别固定各自快照，确认事务失败会回滚文档头与新快照。
27. “来源监测”工作区显示真实健康、周期、下次运行、运行历史、暂停/恢复、重试、版本 Diff 和待审入口；API/worker 分进程运行，Docker Compose 默认启动一个 collector。
28. 专题首页按“待审核/处理中/失败/已确认”组织注意力；当前研判分别展示来源可靠性、独立来源印证、证据完整性和人工研判依据，没有数据时保持未知，不生成综合知情度分数。
29. 专题审核队列默认只暴露本专题材料和合并目标；跨专题复用必须显式授权。确认前提供 Source/Document/Snapshot/Event/Claim/Evidence 与关系/动作的语义预览，确认后返回正式事件入口和下一条任务。
30. RSS/Atom 可靠来源：按目标内 GUID/URL 加内容指纹持久去重；新条目保存来源提供的标题与摘要快照并进入 P0.3 审核，重复项只保留运行计数；私网/无效条目 fail-closed，malformed feed 记录 `rss_parse` 失败，worker 崩溃后可按运行与材料指纹认领已提交条目。

## 当前工作台验收重点

- 专题态势、事件流、时间线和研判面板选择状态一致。
- 点击事件可打开档案抽屉，并在概览、主张证据、来源资料之间切换。
- 每条证据能够回到对应文档快照，并保留事件返回上下文。
- 当前事件能够生成可访问的 HTML 证据简报。
- 网页和 RSS 导入能够入库，并保留来源、抓取时间、正文哈希与重复族标识。
- 关键词、重要性、语言和争议筛选可组合使用。
- 桌面与窄屏布局均可用。
- 在专题内完成“关键词查询 → 多页加载/跨页选择 → 异步逐条处理 → 语义审核 → 正式事件 → 专题报告”，查询历史可重开，未确认候选不进入正式区。
- 错误不仅显示“失败”，还应说明发生阶段、影响和下一步；DNS/SSRF 策略拒绝不可被普通重试按钮绕过。
- 当前研判的每个维度都能展开查看事实依据；未知保持未知，文档数不冒充独立来源数，机器置信度不冒充人工结论。

## 当前结论与下一步

P0.2 已随提交 `7d3f71a` 合入 `master`。P0.3 与最初的 P1 关键词发现已经合入；本轮工作区把关键词发现收敛为专题化查询/处理/审核体验，并保留旧 API 的增量兼容。固定网页可靠采集首切片与 RSS/Atom 条目发现切片也已实现。它们都不冒充完整 P1：首个真实专题包及其统一“今天”输出尚未冻结；可靠持续采集当前仍只支持无需登录的固定 HTTP(S) 网页、RSS 条目摘要和单 collector，通用 API/专题必要的受控浏览器来源、RSS 条目原文追抓、30–50 个真实来源、连续 72 小时、抓取安全与性能基准、PostgreSQL/PostGIS、模型调用成本和最终运行验收报告尚未完成。

外部检索自动验收使用受控替身覆盖真实后端成功、空结果、超时/限流/失败、HTML 注入文本、私网/重定向私网 URL、无正文、重试和幂等，不依赖实时外网。2026-08-28 另完成真实浏览器走查：本地 SearXNG 2026.8.22 返回新闻和一般网页结果；空结果、后端不可达、原始页 401/SSRF 拒绝均显示真实错误；选择 BTS 页面后进入采集箱，详情保留关键词、查询运行、渠道和结果 ID，人工确认生成的正式证据可回到原始快照并高亮原句。外部关键词发现的完整边界和验收见 `docs/product/P1-EXTERNAL-KEYWORD-DISCOVERY.md`；固定网页/RSS 切片之后仍待推进 API/浏览器来源、PostgreSQL/PostGIS、真实来源扩张和后续规模化能力，不提前实现无人值守确认。

2026-08-30 当前工作区新增自动契约覆盖：专题查询历史与隔离、三页累计/跨页去重、未知总数、一次选择 25 条、中文自动语言映射、结构化错误持久化、失败查询原运行重试、DNS fail-closed、SearXNG 运营方大页不被截断、旧数据库增量迁移，以及专题审核范围、跨专题显式复用、语义预览、确认导航和历史候选失败恢复。提交候选必须重新运行 `./scripts/test-p0.sh`、`docker compose config --quiet` 与 `git diff --check`；本段描述验收覆盖，不把尚未发生的 GitHub CI、长期运行或生产外网结果写成已通过证据。

2026-08-30 对当时候选完成本地验收：`./scripts/test-p0.sh` 为 58 项测试通过，Python 编译、前端语法、Shell 语法、`docker compose config --quiet` 与 `git diff --check` 通过。受控 SearXNG 浏览器流程在 1440×900 创建真实持久专题，验证限流错误和历史重开、20+20+5 条跨页累计、全选已加载 45 条、异步逐条处理、44 条 ready 与 1 条 DNS 策略拒绝并存；规则降级候选必须先预览，修改后会撤销预览授权，重新预览并确认后可打开正式事件，空事件时间保持未知，专题“今天”页显示正式证据覆盖和失败项。英文关键词实际向替身发送 `language=en`。390×844 下正文无横向溢出，审核按“选择材料—核对原文—决定入档”三步显示。该流程使用受控检索/正文替身验证产品行为，不冒充生产外网、GitHub CI、长期运行或完整 P1 证据。

2026-08-30 当前工作区新增 RSS/Atom 可靠来源候选：自动契约覆盖两个条目首次发现、同一 feed 重复运行不重复入箱、私网条目无效且不生成材料、malformed feed fail-closed、旧 `collection_targets` 增量迁移，以及 worker 在材料提交后/状态链接前崩溃的租约重放认领。提交候选前必须重新执行完整原生验收；本段不把尚未发生的浏览器走查、GitHub CI 或长期运行写成已通过证据。

2026-08-30 对该 RSS 候选完成最新本地验收：`./scripts/test-p0.sh` 为 87 项测试通过，Python 编译、前端语法、Shell 语法、`docker compose config --quiet` 与 `git diff --check` 通过。浏览器走查使用临时 SQLite 和受控持久数据验证 1440×900 与 390×844：来源类型可选择 RSS/Atom，表单提交后显示持久 queued run，RSS 详情显示新条目/重复/无效计数和发现条目，条目可打开 P0.3 固定快照审核，窄屏正文无横向溢出。该走查不冒充真实外网 feed、GitHub CI、长期运行或完整 P1 证据。

2026-08-29 对固定网页首切片完成最新本地验收：`./scripts/test-p0.sh` 为 38 项测试通过，另通过 Python 编译、前端语法、`docker compose config --quiet` 与 `git diff --check`；以外部数据库地址启动单文件测试时，测试仍强制使用自己的临时库，外部哨兵表保持未改动。真实 API、独立 collector 和临时 SQLite 的浏览器走查中，`example.com` 完成“排队 → V1 → 待审 → 人工确认 → 正式快照”，再次抓取正确记为正文未变；`httpbin.org/uuid` 连续抓取形成 V1/V2，390px 窄屏可查看差异、进入 V2 待审、预览确认并回到对应正式快照，页面无横向溢出。该记录是当前工作区的本地证据，不是 GitHub CI、长期运行或完整 P1 通过声明。

P1 后续先按 `docs/product/FUNCTIONAL-PERFORMANCE-REQUIREMENTS.md` 冻结首个真实专题包和验收口径，再补齐来源类型、30–50 个来源、72 小时运行、安全、性能、迁移和模型成本证据。团队权限与审计、跨媒体转载链和长期许可证决策分别放入 P3、P4 等后续阶段，具体归属和退出条件见 `docs/product/ROADMAP.md`。首切片契约和逐场景验收见 `docs/product/P1-RELIABLE-COLLECTION-SLICE.md`。

## 运行

```bash
./scripts/setup-p0.sh
./scripts/test-p0.sh
./scripts/run-p0.sh
```

可靠采集另开一个终端运行：

```bash
./scripts/run-collector.sh
```

浏览器打开 `http://127.0.0.1:8765`。
